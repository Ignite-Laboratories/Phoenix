package tiny

import (
	"math/big"
)

// Rune is a tiny.Operation[rune]
//
// see.BaselessCalculation
var Rune Operation[rune]

// Byte is a tiny.Operation[byte]
//
// see.BaselessCalculation
var Byte Operation[byte]

// Int is a tiny.Operation[int]
//
// see.BaselessCalculation
var Int Operation[int]

// Int8 is a tiny.Operation[int8]
//
// see.BaselessCalculation
var Int8 Operation[int8]

// Int16 is a tiny.Operation[int16]
//
// see.BaselessCalculation
var Int16 Operation[int16]

// Int32 is a tiny.Operation[int32]
//
// see.BaselessCalculation
var Int32 Operation[int32]

// Int64 is a tiny.Operation[int64]
//
// see.BaselessCalculation
var Int64 Operation[int64]

// Float32 is a tiny.Operation[float32]
//
// see.BaselessCalculation
var Float32 Operation[float32]

// Float64 is a tiny.Operation[float64]
//
// see.BaselessCalculation
var Float64 Operation[float64]

// UInt is a tiny.Operation[uint]
//
// see.BaselessCalculation
var UInt Operation[uint]

// UInt8 is a tiny.Operation[uint8]
//
// see.BaselessCalculation
var UInt8 Operation[uint8]

// UInt16 is a tiny.Operation[uint16]
//
// see.BaselessCalculation
var UInt16 Operation[uint16]

// UInt32 is a tiny.Operation[uint32]
//
// see.BaselessCalculation
var UInt32 Operation[uint32]

// UInt64 is a tiny.Operation[uint64]
//
// see.BaselessCalculation
var UInt64 Operation[uint64]

// UIntPtr is a tiny.Operation[uintptr]
//
// see.BaselessCalculation
var UIntPtr Operation[uintptr]

// Complex64 is a tiny.Operation[complex64]
//
// see.BaselessCalculation
var Complex64 Operation[complex64]

// Complex128 is a tiny.Operation[complex128]
//
// see.BaselessCalculation
var Complex128 Operation[complex128]

// String is a tiny.Operation[string]
//
// see.BaselessCalculation
var String Operation[string]

// BigInt is a tiny.Operation[*big.Int]
//
// see.BaselessCalculation
var BigInt Operation[*big.Int]

// BigRat is a tiny.Operation[*big.Rat]
//
// see.BaselessCalculation
var BigRat Operation[*big.Rat]

// BigFloat is a tiny.Operation[*big.Float]
//
// see.BaselessCalculation
var BigFloat Operation[*big.Float]
